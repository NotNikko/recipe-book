<a href={import.meta.env.BASE_URL.replace(/\/?$/, '/')} class="flex items-center gap-3">

    <div class="logo">
        🍴
    </div>

    <div>

        <h2>Cookbook</h2>

        <p>Your personal recipe collection</p>

    </div>

</a>

<style>
.logo{
    width:48px;
    height:48px;

    display:grid;
    place-items:center;

    border-radius:16px;

    background:var(--color-primary);

    color:white;

    font-size:24px;
}

p{
    margin:0;
    color:var(--color-text-muted);
    font-size:.85rem;
}
</style>